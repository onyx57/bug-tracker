pipeline {
    agent any

    stages {
        stage('Unit Tests - Backend') {
            agent {
                docker {
                    image 'jenkins/golang-junit-agent'
                    reuseNode true 
                }
            }
            steps {
                dir('bugtracker-backend') {
                    sh 'go test ./... -v'
                }
            }
        }
    }
}
